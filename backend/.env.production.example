# Backend Production Environment Variables

# CRITICAL: Never commit this file to version control
# Create this file on your production server with real values

# ====================================
# Server Configuration
# ====================================
PORT=5000
NODE_ENV=production

# ====================================
# MongoDB Configuration (MongoDB Atlas)
# ====================================
# Get this from MongoDB Atlas dashboard:
# 1. Go to https://cloud.mongodb.com
# 2. Select your cluster → Connect → Connect your application
# 3. Copy the connection string and replace <password> and <dbname>
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/happytails?retryWrites=true&w=majority

# Example:
# MONGODB_URI=mongodb+srv://happytails_admin:SecurePass123@cluster0.abc123.mongodb.net/happytails_prod?retryWrites=true&w=majority

# ====================================
# Firebase Configuration (Production)
# ====================================
# Get these from Firebase Console → Project Settings → Service Accounts
# Download the service account JSON and extract these values:
FIREBASE_PROJECT_ID=your_production_project_id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com

# IMPORTANT: Keep the quotes and newlines (\n) in the private key

# ====================================
# Google Gemini AI Configuration
# ====================================
# Get your production API key from:
# https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_production_gemini_api_key

# Example:
# GEMINI_API_KEY=AIzaSyD...

# ====================================
# JWT Configuration
# ====================================
# CRITICAL: Generate a strong, unique secret for production
# Use a random string generator: https://randomkeygen.com/
JWT_SECRET=your_super_secure_production_jwt_secret_change_this
JWT_EXPIRE=7d

# Example (use your own random string):
# JWT_SECRET=8f3K9mL2pQ7vX5nR1tY6wZ4sH8jD0cF

# ====================================
# CORS Configuration
# ====================================
# Add your production frontend URL(s)
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://your-frontend-domain.com,https://www.your-frontend-domain.com

# Examples:
# ALLOWED_ORIGINS=https://happytails.vercel.app
# ALLOWED_ORIGINS=https://happytails.vercel.app,https://www.happytails.com

# ====================================
# Rate Limiting Configuration
# ====================================
# Rate limit window in milliseconds (default: 15 minutes)
RATE_LIMIT_WINDOW_MS=900000
# Maximum requests per window
RATE_LIMIT_MAX_REQUESTS=100
# Guest symptom checker limit
SYMPTOM_CHECKER_GUEST_LIMIT=3

# ====================================
# Optional: Logging Configuration
# ====================================
# LOG_LEVEL=info
# LOG_FILE_PATH=/var/log/happytails

# ====================================
# Optional: Email Configuration (Future)
# ====================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# ====================================
# Security Notes
# ====================================
# 1. Never use development keys in production
# 2. Use strong, unique values for JWT_SECRET
# 3. Restrict ALLOWED_ORIGINS to your actual domain(s)
# 4. Enable MongoDB IP whitelist for your server IP
# 5. Use environment variables on your hosting platform (Railway, Render, etc.)
# 6. Rotate secrets regularly (JWT_SECRET, API keys)
# 7. Monitor logs for suspicious activity
